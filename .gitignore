CLAUDE.md
AGENTS.md
.env
.env.bak-*
__pycache__/
*.pyc
images/*
annotations.db
annotations.db-wal
annotations.db-shm
.history/
.claude/
.DS_Store
.gradio/certificate.pem
# Local-only test scripts. The first pattern used to read `test_games.py`,
# which matched nothing — the real filename has a leading underscore, so
# the file stayed tracked despite e5edc98 meaning to drop it.
_test_games.py
_test_assignment.py

# Pre-study transcript samples. No code reads these any more (training.py
# moved to games_practice/); kept on disk, out of the repo.
interactions/
exports/

# Raw clembench dump that games_study/ is built FROM (85MB, 6k files).
# The curated study set is committed; this is not. Keep the original archive
# safe — build_study_set.py needs it to regenerate or re-seed the selection.
games_final/
